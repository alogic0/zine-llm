# Incident Memory

Use this folder for significant release/build regressions, release-blocking
failures, corrupted published artifacts, or other failures that merit a
post-fix record.

Ordinary test failures found and fixed during implementation do not require an
incident note. The note should explain impact, root cause, resolution, and the
guard that prevents recurrence.

Start with `docs/templates/incident-note.md`.
